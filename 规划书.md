# 多巴胺 (Duobaan) 平台规划书

> 版本：v1.0 ｜ 项目目录：`duobaan` ｜ 后端基座：Spring Boot 4.1.1 / Java 17
> 定位：一个把「办公协作」与「日常生活多巴胺释放」统一在同一个界面里的个人效率 + 生活助手平台。

---

## 一、项目概述

### 1.1 一句话定位
**一个界面、两个模式、一个大模型**——上班通勤规划任务、下班吃饭决策，都在「多巴胺」平台里完成。

### 1.2 解决的问题
- **办公侧**：每天到岗/离岗时需要规划今日/明日安排，任务分散在脑子里和多个工具里，缺少一个能被大模型驱动、可勾选上交的「流程表」。
- **生活侧**：到饭点常常不知道吃什么——懒得想、没时间想、纠结口味/天气/心情/用餐方式（外卖/堂吃/自己做）。把这些变量交给大模型算一餐建议，决策成本降到接近零。
- **统一侧**：办公工具和生活工具割裂，来回切换损耗注意力。本平台用「左侧两个按钮（办公 / 多巴胺）」一键切换两个模式，共享同一套天气/时间/大模型/流程表底座。

### 1.3 核心价值
| 维度 | 能力 |
|---|---|
| 实时感知 | 实时天气 + 实时时间，作为大模型推理与界面氛围的输入 |
| 大模型接口 | 统一接入大模型，驱动办公任务生成与餐食推荐 |
| 流程表 | 任务的规划、勾选完成、上交，作为大模型上下文与进度看板 |
| 双模式 UI | 办公模式 / 多巴胺模式，左侧按钮一键切换 |

---

## 二、用户场景与典型流程

### 2.1 场景 A：早晨到岗，规划今天
1. 打开平台 → 默认进入「办公模式」大模型对话页。
2. 顶栏显示当天天气（晴/雨/温度）与当前时间。
3. 用户在对话区输入：「今天要交周报、还要评审两段代码、回三个客户邮件」。
4. 大模型读取天气/时间上下文 + 现有流程表，**自动把这三件事拆成流程表条目**，写入右侧流程表。
5. 用户在流程表逐项勾选完成，完成的条目状态变「完成」并计入「今日完成数」。

### 2.2 场景 B：下班前，规划明天
1. 临近下班，用户对大模型说：「把没做完的挪到明天，再排一个早上 9 点的站会」。
2. 大模型把未完成项迁移到「明日」区，并新增站会条目。
3. 流程表区分「今日 / 明日」两组，用户上交今日工作小结。

### 2.3 场景 C：饭点，不知道吃什么
1. 点左侧「多巴胺」按钮 → 进入多巴胺释放完成页。
2. 顶栏已带入当前天气（如「小雨 19℃」），用户再选/输入：心情（略累）、口味（酸辣）、用餐方式（外卖/堂吃/自己做）。
3. 大模型综合天气 + 心情 + 口味 + 用餐方式，**计算一餐推荐**（如「酸汤肥牛 + 米饭，堂吃，步行 8 分钟」）。
4. 用户采纳后，该餐自动写入流程表「今日用餐」并标记完成，形成「多巴胺释放」闭环。

### 2.4 场景 D：直接在平台办办公任务
接入大模型后，用户在办公模式对话区直接下达任务（起草周报、总结会议纪要、生成回复话术），大模型产出结果，用户确认后可一键转成流程表条目上交。

---

## 三、功能需求

### 3.1 功能模块总览
```
多巴胺平台
├── 公共能力
│   ├── 实时天气
│   ├── 实时时间
│   ├── 大模型接口（统一网关）
│   └── 流程表（今日 / 明日，状态流转，上交）
├── 办公模式（页面一：大模型对话任务页）
│   ├── 对话区（下达任务 / 起草 / 总结）
│   ├── 流程表区（右侧，自动/手动条目）
│   └── 今日小结上交
└── 多巴胺模式（页面二：多巴胺释放完成页）
    ├── 情境输入（心情 / 天气 / 口味 / 用餐方式）
    ├── 大模型餐食推荐
    └── 推荐采纳 → 写入流程表「今日用餐」
```

### 3.2 公共能力

#### 3.2.1 实时天气
- 数据源：第三方天气 API（如和风天气 / OpenWeather，二选一，配 API Key）。
- 字段：城市、天气现象、温度、体感温度、风力、更新时间。
- 缓存：后端按城市缓存（建议 10 分钟），避免高频调用。
- 用途：顶栏展示 + 作为大模型 prompt 上下文 + 多巴胺推荐输入。

#### 3.2.2 实时时间
- 后端统一时间源，前端顶栏每秒/每分钟刷新。
- 字段：当前时间、星期、是否工作日、今日/明日标识。

#### 3.2.3 大模型接口（统一网关）
- 后端封装统一 `/api/llm/chat` 入口，屏蔽具体模型厂商。
- 支持配置切换模型与 API Key（写在 `application.properties`，不入库）。
- 系统提示词分两类：**办公 Agent**、**多巴胺餐食 Agent**，按当前模式加载。
- 流式输出（SSE）优先，提升对话体感。

#### 3.2.4 流程表
- 容器：今日 / 明日两组。
- 条目字段：标题、分类（工作/用餐/自定义）、状态（待办/进行中/完成/已上交）、预计时长、截止时间、来源（手动/大模型生成）。
- 操作：新增、勾选完成、迁移今日↔明日、上交（把当日完成项打包成小结）。
- 与大模型双向打通：大模型可写条目，条目状态也作为大模型上下文。

### 3.3 办公模式（页面一）
- **左侧**：模式切换按钮（办公/多巴胺）。
- **中央**：大模型对话区，支持下达任务、起草、总结，结果可一键转流程表条目。
- **右侧**：流程表，今日/明日分组，状态流转与上交。
- **顶栏**：天气 + 时间 + 今日/明日任务计数。

### 3.4 多巴胺模式（页面二）
- **左侧**：模式切换按钮（办公/多巴胺）。
- **顶栏**：天气 + 时间 + 当前心情/口味标识。
- **中央**：
  - 情境输入区：心情（选择/文本）、口味（标签多选）、用餐方式（外卖/堂吃/自己做）。
  - 大模型推荐卡片：餐食名、理由（结合天气/心情/口味）、就近建议、预计花费。
  - 采纳按钮 → 写入流程表「今日用餐」并标记完成（多巴胺释放闭环）。

---

## 四、界面设计

### 4.1 整体结构
两页统一框架：
- 顶栏（Top Bar）：左 Logo，中 天气+时间，右 模式指示与今日/明日计数。
- 左侧栏（Side Rail）：两个按钮——「办公」「多巴胺」，当前模式高亮。
- 主内容区：随模式切换两套布局。

### 4.2 页面一：大模型对话任务页
三栏布局（已在上方布局示意中可视化）：
| 区域 | 宽度 | 内容 |
|---|---|---|
| 左侧 | 窄 | 办公 / 多巴胺 两个按钮 |
| 中央 | 自适应 | 大模型对话流 + 输入框 + 「转为流程表条目」按钮 |
| 右侧 | 中 | 流程表：今日组 / 明日组，条目状态与上交 |

### 4.3 页面二：多巴胺释放完成页
两栏布局：
| 区域 | 宽度 | 内容 |
|---|---|---|
| 左侧 | 窄 | 办公 / 多巴胺 两个按钮 |
| 中央 | 自适应 | 天气/心情/口味/用餐方式输入 → 大模型餐食推荐卡 → 采纳写入流程表 |

### 4.4 设计基调
- 扁平、克制、信息密度适中；办公模式偏冷静（品牌主色），多巴胺模式偏暖（辅色点缀）。
- 天气作为氛围输入但不喧宾夺主（如雨天可在顶栏加一个雨滴指示，不改全局背景）。

---

## 五、系统架构

### 5.1 架构总览
```
┌────────────────────────── 前端 SPA (Vue 3) ──────────────────────────┐
│  办公模式页  /  多巴胺模式页  /  顶栏天气时间  /  左侧模式切换            │
└───────────────┬───────────────────────────────┬──────────────────────┘
                │ REST + SSE                      │
┌───────────────▼───────────────────────────────▼──────────────────────┐
│                  Spring Boot 4.1.1 (duobaan 后端)                     │
│  ┌─────────────┐ ┌─────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │ 天气服务    │ │ 时间服务     │ │ 大模型网关    │ │ 流程表服务    │  │
│  │ (缓存/代理) │ │ (统一时钟)   │ │ (SSE/双 Agent)│ │ (CRUD/上交)  │  │
│  └──────┬──────┘ └─────────────┘ └──────┬───────┘ └──────┬───────┘  │
│         │                                 │                │          │
└─────────┼─────────────────────────────────┼────────────────┼──────────┘
          │                                  │                │
   第三方天气 API                       大模型厂商 API     H2 / SQLite (本地)
```

### 5.2 后端分层
- `controller`：`WeatherController`、`TimeController`、`LlmController`、`TaskController`。
- `service`：`WeatherService`（带缓存）、`LlmGatewayService`（统一网关 + 双系统提示词）、`TaskService`（流程表 CRUD + 上交）、`RecommendService`（多巴胺餐食推荐编排）。
- `model`：`Task`、`TaskGroup`（今日/明日）、`MealContext`、`ChatMessage`。
- `config`：大模型 API Key、天气 API Key、缓存策略。

### 5.3 前端结构（建议 SPA）
- 技术栈：Vue 3 + Vite + Pinia + 轻量 UI 组件。
- 两个页面（路由）：`/work`（办公）、`/dopamine`（多巴胺）；共享 `TopBar` 与 `SideRail` 组件。
- 与后端通过 REST 同步流程表，通过 SSE 接收大模型流式回复。

---

## 六、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Spring Boot 4.1.1（已存在） | 项目基座已定 |
| JDK | Java 17（已存在） | 与 Spring Boot 4 兼容 |
| 持久层 | Spring Data JPA + H2（本地）/ SQLite | 个人工具，轻量优先，可后换 MySQL |
| 大模型接入 | OpenAI 兼容协议（可配厂商） | 主流大模型均提供兼容接口，便于切换 |
| 天气数据 | 和风天气 / OpenWeather | 二选一，按 Key 可用性决定 |
| 前端 | Vue 3 + Vite | UI 交互复杂，SPA 体验好；可由 Spring Boot 托管静态资源 |
| 实时通信 | SSE（大模型流式） | 单向流式，比 WebSocket 轻 |
| 构建 | Maven（已存在 mvnw） | 项目已配 |

---

## 七、数据模型

### 7.1 流程表条目 Task
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Long | 主键 |
| title | String | 任务标题 |
| category | Enum | WORK / MEAL / CUSTOM |
| group | Enum | TODAY / TOMORROW |
| status | Enum | TODO / DOING / DONE / SUBMITTED |
| estimatedMinutes | Integer | 预计时长 |
| dueAt | LocalDateTime | 截止时间 |
| source | Enum | MANUAL / LLM |
| createdAt | LocalDateTime | 创建时间 |

### 7.2 餐食情境 MealContext
| 字段 | 类型 | 说明 |
|---|---|---|
| mood | String | 心情（略累/兴奋/平淡…） |
| weather | String | 当前天气（来自天气服务） |
| tasteTags | List<String> | 口味（酸辣/清淡/甜…） |
| diningType | Enum | TAKEOUT / DINE_IN / COOK |

### 7.3 对话记录 ChatMessage
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Long | 主键 |
| role | Enum | USER / ASSISTANT |
| mode | Enum | WORK / DOPAMINE |
| content | Text | 内容 |
| createdAt | LocalDateTime | 时间 |

---

## 八、接口设计（REST + SSE）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/weather/now` | 实时天气（后端缓存） |
| GET | `/api/time/now` | 实时时间 |
| GET | `/api/tasks?group=TODAY` | 取流程表（今日/明日） |
| POST | `/api/tasks` | 新增条目（手动/大模型生成） |
| PATCH | `/api/tasks/{id}` | 更新状态/迁移分组 |
| POST | `/api/tasks/submit` | 上交今日小结 |
| POST(stream) | `/api/llm/chat/work` | 办公模式对话（SSE） |
| POST(stream) | `/api/llm/chat/dopamine` | 多巴胺模式餐食推荐（SSE） |
| POST | `/api/recommend/meal` | 提交 MealContext，触发推荐编排 |

---

## 九、大模型接入方案

### 9.1 统一网关
- `LlmGatewayService` 统一封装：鉴权、超时、重试、流式解析、错误降级。
- 配置项（`application.properties`）：
  - `duobaan.llm.base-url`
  - `duobaan.llm.api-key`
  - `duobaan.llm.model`
  - `duobaan.weather.api-key`、`duobaan.weather.provider`

### 9.2 双系统提示词
- **办公 Agent**：角色为个人工作助理；输入含天气/时间/今日流程表条目；产出可执行安排，并能把自然语言指令拆成流程表条目（结构化 JSON）。
- **多巴胺餐食 Agent**：角色为生活决策助手；输入含天气/心情/口味/用餐方式；产出一餐推荐 + 理由 + 就近建议，并返回结构化字段供前端渲染卡片。

### 9.3 上下文拼接
- 每次调用大模型前，后端把当前天气、时间、当前流程表条目摘要拼进系统提示词，使大模型建议贴合「今天」的实际情况。

### 9.4 安全
- API Key 仅存后端，前端不接触；前端只调本平台 REST/SSE。

---

## 十、实施计划（里程碑）

> 按「先打通骨架，再补功能」的顺序推进；每个里程碑结束都要能本地跑起来。

### 里程碑 1：后端骨架与公共能力
- 搭建 `controller / service / model / config` 分层。
- 实时时间接口、实时天气接口（含缓存）。
- 大模型统一网关（非流式先跑通）。
- 流程表 CRUD（今日/明日/状态流转）。
- **验收**：Postman/curl 能调通天气、时间、流程表、单轮大模型对话。

### 里程碑 2：前端双页骨架
- Vue 3 + Vite 工程，Spring Boot 托管静态资源。
- 顶栏（天气+时间）、左侧模式切换、两个路由页面。
- 办公页三栏布局 + 多巴胺页两栏布局（先用静态假数据）。
- **验收**：两个页面可切换，布局与上方示意一致。

### 里程碑 3：办公模式打通
- 对话区接 SSE 流式回复。
- 大模型把自然语言指令拆成流程表条目（结构化），一键写入右侧流程表。
- 流程表勾选完成、今日↔明日迁移、上交小结。
- **验收**：完成场景 A、场景 B 全流程。

### 里程碑 4：多巴胺模式打通
- 情境输入（心情/天气带入/口味/用餐方式）。
- 大模型餐食推荐卡片渲染。
- 采纳 → 写入流程表「今日用餐」并标记完成。
- **验收**：完成场景 C 全流程。

### 里程碑 5：体验打磨与扩展点
- 天气氛围指示、空状态、加载态、错误降级。
- 对话记录持久化。
- 预留：自定义口味/心情标签、流程表自定义分类、多日视图。
- **验收**：端到端走查场景 A–D 无明显阻塞。

---

## 十一、约束与后续扩展

- 本期为个人工具，数据本地化（H2/SQLite），不引入用户体系。
- 大模型与天气 Key 由使用者自配，文档需给出获取指引。
- 扩展点（非本期）：多用户与云同步、流程表多日/周视图、餐食历史与偏好学习、办公任务与日历/IM 联动。

---

### 附：与原稿要点对照
- 实时天气 / 时间 —— 见 3.2.1 / 3.2.2 / 顶栏。
- 大模型接口 —— 见 3.2.3 / 第九章。
- 流程表（完成与上交） —— 见 3.2.4 / 第七章 Task 模型。
- 上下班规划今天/明天 —— 见 2.1 / 2.2 / TaskGroup(TODAY/TOMORROW)。
- 心情/天气/口味/用餐方式 → 大模型算一餐 —— 见 2.3 / 3.4 / MealContext。
- 接入大模型后在平台办办公任务 —— 见 2.4 / 3.3。
- UI 两页（对话任务页 + 多巴胺释放页）—— 见第四章 / 上方布局示意。
- 流程表在对话页右侧、左侧两按钮选办公/多巴胺 —— 见 4.2 / 4.3 / 布局示意。
